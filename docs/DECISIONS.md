# 제품 및 구현 결정

## 2026-09-06 — 1대1 디베이트 토너먼트로 전환

사용자는 전체 UI 개선을 요청했고, 작업 중 다대다 토론보다 1대1 토너먼트가 더 재미있겠다는 방향을 제시했다. OpenRouter 키는 나중에 제공하며, OpenRouter 중심의 다양한 모델 선택을 원했다.

- 기본 경험은 4강, 확장 선택지는 8강의 단판 토너먼트다.
- 모바일을 우선하되 데스크톱에 전체 대진표와 넓은 탐색 화면을 제공한다.
- 초기 스타일은 종이색·짙은 그린·파스텔이었다. 후속 디자인 결정에서 스포츠·캐릭터 중심 컵 테마로 발전시켰다. 기존 브랜드 로고는 유지한다.
- 모델 검색, 제공사 필터, 무료 모델 필터, 이름/출력 가격 정렬을 지원한다. 추정 인기 순위는 만들지 않는다.
- 키가 없어도 주제 탐색·북마크·모델 선택·대진표 생성까지 가능하다.
- 토론자와 심판 모두 OpenRouter로 실행한다. 별도 Google 키를 요구하지 않는다.
- 선택된 모델을 다른 모델로 자동 대체하지 않는다. 실패한 경기에는 우승자나 진출자를 만들지 않는다.
- 모델 이름은 심판에게 보내지 않는다. 심판은 사용자가 지정하며 참가 모델과 같으면 이를 안내한다.
- 각 경기는 사용자가 직접 시작한다. UI에는 발언·심판 호출 횟수와 토큰 가격을 표시한다.
- 기록을 자동 삭제하지 않는다. 삭제는 명시 확인을 받고, 별도 Markdown 내보내기를 제공한다.
- 기존 캐릭터 토론과 기록을 유지해 기존 사용자의 데이터 접근을 보장한다.

## 2026-09-06 — 귀여운 선수들이 출전하는 왈가왈부컵

사용자는 전체적으로 월드컵·스포츠 느낌을 더하고, 기존 에셋을 최대한 활용해 귀여운 캐릭터성이 먼저 느껴지도록 요청했다.

- 기존 5개 모델 캐릭터와 MC, 브랜드 로고를 재사용한다. 중앙 팀 매핑으로 모델 선택·대진표·기록의 프로필을 일치시킨다.
- 메인은 선수 카드의 등번호·VS·잔디 코트·트로피와 3단계 경기 입장권으로 구성한다. 캐릭터 팀 소개를 추가한다.
- 제목은 둥근 Jua, 본문은 Pretendard. 진한 그린·노랑·산호·블루를 사용해 승부감과 귀여움을 함께 표현한다.
- 원본 그림은 덮어쓰거나 편집하지 않는다. 다른 제공사는 기존 브랜드 캐릭터 로고를 사용하며, OpenRouter 모델 선택에는 제한을 추가하지 않는다.
- 메인의 팀 대결은 예시다. 임의 능력치·순위·관중 수·라이브 경기는 만들지 않는다.

## 검증에서 보강한 부분

- 320px 페이지 여백(16px)과 대진표의 바깥 확장 여백을 일치시켜 5px 넘침을 제거했다.
- 저장된 기존 설정은 hydration 이후 편집 상태로 초기화해 첫 렌더링과 복원 시점의 불일치를 줄인다.
- 모달은 native dialog를 사용하고 닫을 때 원래 버튼으로 포커스를 돌린다.
- 경기 runId로 늦은 응답·중단된 실행·중복 판정이 다음 대진표를 변경하지 못하게 한다.


## 2026-09-06 — 운영자가 AI를 제공하는 방식으로 전환

사용자는 OpenRouter 등의 개인 키 입력 기능을 없애고 모든 AI 연결을 서비스가 제공하도록 요청했다. 위의 개인 키·가격 표시 관련 결정은 이 항목으로 대체한다.

- 키 입력 화면·저장소·설정 탐색을 제거하고 `/settings`는 이용 가이드로 연결한다.
- 클라이언트 요청에는 키를 포함하지 않는다. 토너먼트는 키 필드가 포함된 요청을 거부한다.
- 토너먼트와 기존 캐릭터 토론 모두 서버 OpenRouter 연결을 공유한다. 서버 미설정은 503이며 이용자에게 설정을 요구하거나 모의 경기를 생성하지 않는다.
- 모델 선택은 이름·제공사 검색으로 제공한다. 외부 토큰 가격·무료 필터·크레딧 안내는 제거한다. 운영자의 선택적 제공 모델 목록은 서버에서도 검증한다.
- 기존 캐릭터는 실제 OpenRouter 카탈로그에서 확인한 제공사별 모델 ID를 사용한다. 모델 ID는 `src/lib/ai/config.ts`에 모은다.
- 이전 브라우저 키만 삭제하고 저장한 대진표·토론 기록·북마크는 보존한다.

## 2026-09-06 — PR 리뷰 이후 로그인과 운영 한도

사용자 요청에 따라 기존 UI·토너먼트·캐릭터 20종을 PR #1로 분리하고 Codex 리뷰 완료 후 `codex/service-auth-and-limits`에서 후속 구현한다. PR #1의 979eb6d는 주요 지적 없이 리뷰를 완료했으며 merge와 운영 배포는 별도다.

- Google 로그인과 계정 이용 내역을 Supabase Auth/Postgres로 구현한다. 전용 프로젝트를 권장하며 기존 AIB 프로젝트 통합 여부는 미확정이다. 외부 프로젝트를 임의 변경하지 않는다.
- 공개 탐색과 대진 생성은 로그인 없이 제공하고 AI 실행은 검증된 계정만 허용한다.
- 서버 원장과 원자적 RPC로 계정별 일일 한도와 중복 실행을 제어한다. 초기에는 Redis를 추가하지 않는다.
- 실패 시에도 시작한 요청 횟수를 보존한다. DB 장애에서 유료 호출을 허용하던 이전 fallback을 제거한다.
- 운영 활성화 플래그, OpenRouter 키, 비어 있지 않은 모델 허용 목록이 모두 있어야 실행한다. 전역 비용 상한은 OpenRouter 대시보드에서 함께 설정한다.
- 대진표는 브라우저에 유지하며 서버에는 사용량과 완료 응답 재사용에 필요한 원장을 저장한다. 실제 OAuth/AI 실호출, 기기 간 기록 동기화, 비용 자동 대사와 보존·탈퇴 정책은 후속 운영 설정 범위다.

## 2026-09-07 대표 선수 15명과 스포츠 스카우팅 카드

사용자가 제시한 OpenRouter 모델 15개를 전부 대표 선수로 채택한다. 8개 추천안으로 축소하지 않는다. `src/lib/players.ts`가 정확한 모델 ID, 국가(개발사 출신 기준), 기존 캐릭터, 스카우팅 자료의 기준이다. 새 이미지를 만들거나 원본을 수정하지 않고 20종 중 서로 다른 15종을 연결하며 남은 5종도 컬렉션에 보존한다.

`/characters`에서 국가·모델/캐릭터 이름으로 탐색하고 최대 8명을 선택한다. 선택한 모델 ID를 URL로 `/tournament/new`에 넘겨 4강/8강 대진을 준비한다. 실제 선택 가능 목록은 정적 스카우팅 자료가 아니라 `/api/models`의 현재 카탈로그와 운영자 허용 목록의 교집합이다. 새 매칭은 메인 선수 카드와 ModelAvatar를 통해 대진·토론·우승까지 공유한다.

### 자료 기준

- 확인일: **2026-09-07**, 실시간 점수 아님
- 가격과 지능: [OpenRouter 공개 모델 카탈로그](https://openrouter.ai/api/v1/models)의 `pricing.prompt/completion`, `benchmarks.artificial_analysis.intelligence_index`. 코딩 점수로 지능 점수를 대체하지 않는다.
- 속도: 각 OpenRouter 모델 상세 페이지에 표시된 특정 제공 경로의 **최근 1주 P50 생성 토큰/초**. API의 `throughput_last_30m`은 수집 당시 null이므로 이 필드를 사용했다고 표기하지 않는다. 경로와 모델별 속도를 각 카드에 표시한다. 서로 다른 제공 경로이며 응답 시작 지연(TTFT), 추론 토큰 소모, 실제 토론 소요 시간은 이 수치와 다르다.
- 원본 지능 점수는 7개만 공개되어 있다. 나머지 8개는 null이며 레이더에서도 해당 점과 연결선을 생략한다. 0점·중간값으로 그리지 않는다.
- 브랜드와 개성은 **재미용 편집 설정**이다. 인지도 조사를 했거나 공식 성능 순위인 것처럼 표시하지 않는다. 별표와 설명으로 실측 축과 구분한다.
- 국가 예시 출처: [Arcee 미국 모델 연구소](https://www.arcee.ai/about), [Upstage 한국 기업 정보](https://www.upstage.ai/ko), [Sakana 일본 회사 정보](https://sakana.ai/company-info/). 국가는 추론 서버 위치가 아니다.

### 레이더 환산

각 축은 0–100 눈금으로 제한한다. 속도 = tokens/s ÷ 150 × 100. 가격 = 100 ÷ (1 + 평균 단가 ÷ 2), 평균 단가 = (입력 단가 × 2 + 출력 단가) ÷ 3 (USD / 1M tokens). 저렴할수록 높다. 지능 = AA 원점수 ÷ 60 × 100. 가격·속도는 프로모션이나 연결 경로에 따라 바뀔 수 있으며 한국어 토론 승률을 나타내지 않는다. 원자료·제공 경로·출처 링크는 카드의 접이식 영역에서 볼 수 있다.

### 모델과 캐릭터

| AI 모델 | 캐릭터 | 국가 |
| --- | --- | --- |
| Gemini 3.8 Flash | 모모 | 미국 |
| Claude Haiku 4.5 | 누리 | 미국 |
| GPT-5.6 Luna | 루미 | 미국 |
| Grok 4.6 | 스파크 | 미국 |
| DeepSeek V4 Pro | 미로 | 중국 |
| Qwen3.8 Flash | 차차 | 중국 |
| Kimi K2.6 | 파도 | 중국 |
| Solar Pro 4 | 솔 | 한국 |
| Trinity Large Thinking | 루프 | 미국 |
| Nemotron 3 Ultra | 네오 | 미국 |
| Hy4 | 도도 | 중국 |
| GLM 5.3 Flash | 픽 | 중국 |
| MiniMax M3 | 콩 | 중국 |
| Muse Spark 1.3 | 유니 | 미국 |
| Sakana Namazu | 젤리 | 일본 |

토리·포포·밤비·탱고·비비는 아직 모델을 연결하지 않은 예비 캐릭터다. 모델 버전이 바뀌면 대표 ID와 측정 자료를 명시적으로 갱신한다. 자동으로 다른 모델에 선수 카드를 재연결하지 않는다.

### 선수 지표 명칭과 공식 아이콘

2026-09-07 후속 요청으로 UI의 ‘가격’ 축을 ‘몸값’, ‘브랜드’ 축을 ‘지명도’로 변경했다. 기존 환산식은 유지하며 몸값은 저렴할수록 높다. 지명도·개성은 재미 지표로 표시한다. 15명 모델명에는 `public/logos`의 사용자 제공 공식 아이콘을 연결한다. Gemini는 반드시 `gemini.png`, Sakana는 `sakanaai.png`를 사용하며 모델 선택과 메인 소개에서도 같은 로고 매핑을 공유한다.

### 메인 히어로 크기와 아이콘 정리

상단 문구는 “Let's Debate ・ 최신 AI 들의 진검 토론 승부”. 제목 위 진검승부 문구와 1:1 배지는 삭제한다. 제목과 두 CTA를 확대하고 CTA 글자 크기는 동일하게 유지한다. 대표 5팀은 테두리·배경·이름 없이 공식 아이콘만 표시하되 접근성 이름은 보존한다. ‘전체 15팀’ 링크는 버튼 형태를 유지한다. 빌드 및 320–1559px 화면 검증 통과.

### 히어로 후속 정렬과 영문 브랜드

모델 아이콘 옆 이름을 복원하되 배경·테두리는 없게 유지한다. 전체 15팀 버튼은 확대하고 보조 CTA는 ‘토론 주제 고르기’로 표시한다. 왼쪽 소개를 위로 조정하고 CTA 위 간격을 늘린다. 화면과 메타데이터의 영문 브랜드는 Let's Debate AI로 통일한다. 브라우저 저장 키와 기존 URL 식별자는 표시 브랜드와 별도로 유지한다.


## Artificial Analysis 지능 자료 보완 (2026-09-07)

기존 OpenRouter 지능 누락 5건을 사용자가 제공한 AA 모델 페이지의 v4.2 지표로 보완했다. 원점수는 기존 방식인 원점수 / 60 × 100으로 레이더에 환산한다. 기존 OpenRouter 스냅샷과 평가 버전·설정이 다를 수 있으므로 안내와 원자료 조건을 표시한다. 미확인 3개 지능 값은 null을 유지한다.

|모델|AA 원점수|조건 및 출처|
|---|---:|---|
|Kimi K2.6|36|[추정치 · reasoning](https://artificialanalysis.ai/models/kimi-k2-6)|
|Solar Pro 4|33|[추정치](https://artificialanalysis.ai/models/solar-pro4)|
|Trinity Large Thinking|12|[추정치](https://artificialanalysis.ai/models/trinity-large-thinking)|
|Nemotron 3 Ultra|29|[Reasoning](https://artificialanalysis.ai/models/nvidia-nemotron-3-ultra-550b-a55b)|
|Muse Spark 1.3|53|[max 설정](https://artificialanalysis.ai/models/muse-spark-1-3)|

검증: 30개 테스트, 선수 UI 기능 검사, 빌드와 lint 통과. 반응형 가로 넘침 및 카드 레이더 시각 검토 완료.


## 대표 캐릭터 교환 (2026-09-07)

GPT는 미로, Claude는 네오, Gemini는 차차로 변경한다. 15종의 고유 매칭을 유지하도록 기존 짝을 교환해 DeepSeek은 루미, NVIDIA는 누리, Qwen은 모모를 배정했다. 공통 매핑으로 홈·프로필·대결 아바타에 반영된다. 나머지 매칭은 유지한다. 매핑 검사와 빌드 통과.

### 선수 목록 정렬 (2026-09-07)
- 정렬 버튼: 이름순(모델명), 국가순(한국어 국가명, 같은 국가에서는 모델명), 추천순(Gemini → GPT → Grok → DeepSeek → Solar → Nemotron → Muse → Claude, 나머지는 기존 순서). 기본 추천순.
- 검색·국가 필터와 함께 적용하며 선택한 라인업의 순서는 유지한다.
- 빌드 및 선수 UI 검사 통과: 정렬 전환, 국가 필터 조합, 추천순 복원, Escape 닫기, 모바일 폭 검사.

### 선수 목록 배치 정리 (2026-09-07)
- 검색창 바로 옆에 정렬 버튼 배치. 좁은 화면에서도 검색·정렬을 한 줄로 묶는다.
- 후보 선수 제목은 “출전 대기 중인 후보 선수들”. 후보 영역 하단 테두리를 제거해 푸터 구분선과 중복되지 않도록 한다.

### 후보 선수 상시 표시와 컨트롤 통일 (2026-09-07)
- 후보 영역의 details/summary를 section/h2로 바꿔 접기 기능과 + 아이콘 제거. 후보 5명 항상 표시.
- 검색·정렬·국가 버튼 높이 50px, 대표 선수 수 글자 15px로 통일·확대.
- 빌드 및 320/390/1559px 화면에서 높이·가로 넘침·후보 표시 확인.

### 로고 색감에 따른 캐릭터 배치 (2026-09-07)
- 사용자 지정 GPT–미로, Claude–네오, Gemini–차차 고정.
- DeepSeek–밤비(청보라), Solar–유니(보라), Nemotron–콩(연두), Muse–파도(파랑), Hy4–누리(하늘), MiniMax–비비(진분홍), Kimi–도도(짙은색). 나머지 유지.
- 밤비·비비 출전, 루미·솔 후보 이동. 15명 중복 없이 유지하며 공유 매핑으로 홈과 대결 아바타에도 반영.
- 노란 패스의 15는 기울임과 음수 자간을 없애 중앙 정렬. 문구는 “각 나라의 국가 대표 AI / 1대1 토론 배틀” 두 줄. 소개 문장에서 “중,” 쉼표 제거.

### Claude 캐릭터 변경 (2026-09-07)
- 사용자 요청으로 Claude를 네오에서 후보 토리(07-tori)로 변경. 네오는 후보로 이동. 공유 매핑으로 홈·선수 카드·대결 아바타에 반영. 빌드와 캐릭터 매핑 검사 통과.
